Drop custom Principality artwork here as PNG, WebP, or SVG files.

File name must match the principality id from the map, for example:
  jealousy.png
  destructive-attitudes-against-gods-image.svg

If no custom file exists, the map uses a generated tile with initials.

Rebuild is not required — refresh the browser after adding images.
